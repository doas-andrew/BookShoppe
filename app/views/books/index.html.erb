<div class="view y-scroll">
	<div class="view_content">
		<h1>Book Search</h1>

		<!-- display all books -->
		<% if @books %>
			<font size="3"><br>These are all of the books in our system:</font><hr>
			<% @books.each do |book| %>
				<h3><%= link_to book.slug, books_path+"?search=#{book.title}" %></h3>
				<font size="3" style="margin-left: 1%;"><%= user_search_count(book.num_available.count) %></font><br><br><br>
			<% end %>

		<!-- display one book -->
		<% elsif @book %><br><hr>
			<h3><%= @book.slug %></h3>
			<font size="3" style="margin-left: 1%;"><%= user_search_count(@book.num_available.count) %></font><br><br>

			<div style="margin-left: 2%;">
			<% @book.num_available.each do |user| %>
				<a href="<%= user_path(user) %>" class="btn blue_btn large_btn"><i class="user icon"></i> <%= user.username %></a><br><br>
			<% end %></div>

		<!-- display author's books -->
		<% elsif @author %><br><hr>
			<h3>Books written by <%= @author.name.titleize %>:</h3><br>
			<% @author.books.each do |book| %>
				<h3><%= link_to book.slug, books_path+"?search=#{book.title}" %></h3>
				<font size="3" style="margin-left: 1%;"><%= user_search_count(book.num_available.count) %></font><br><br><br>
			<% end %>
			
		<!-- display no books -->
		<% else %>
			<font size="3">We couldn't find any books in our system based on your search criteria.</font><hr>

		<% end %>
	</div>
</div>